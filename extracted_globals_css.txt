"# Implementation Plan: Founder Detail Profile View\n\nImplement a full-screen, premium detail profile view for founders that opens when a user clicks on any founder in the *Founder Spotlight* section. The layout will replicate the provided mockup exactly.\n\n## User Review Required\n\n> [!IMPORTANT]\n> - Clicking on any founder in the *Founder Spotlight* will now open a dedicated sub-view within the `.content-container`.\n> - Users can return to the main dashboard by clicking \"Home\" in the breadcrumbs or any sidebar item.\n> - Detailed, realistic data will be populated for all four founders (*Sam Altman, Dario Amodei, Aravind Srinivas, Michael Truell*) to ensure compilation and visual parity.\n\n## Proposed Changes\n\n### Component Design\n\nWe will introduce a state-driven view rendering inside `page.tsx`:\n* `selectedFounder`: stores the details of the active founder profile being viewed.\n* If `selectedFounder` is active, render the `<FounderProfileView founder={selectedFounder} onClose={() => setSelectedFounder(null)} />` component inside the main `.content-container` (replacing the dashboard grids).\n\n### CSS Styling [globals.css](file:///Users/sam/Projects/Orbit-Internship/src/app/globals.css)\n\nCreate a comprehensive design system for the profile page matching the mockup aesthetics:\n* **Layout**: CSS grids splitting the profile card into the main left column (Profile card, metrics, timeline, graph, top investments, media) and the right column sidebar (Key at a glance, what they are building, core focus, recent activity, connect, people also viewed).\n* **Influence Graph**: A visual canvas or SVG connecting the founder's avatar to related company icons with dotted/dashed colored lines.\n* **Timeline styling**: Vertical stepper line with years and company logo badges.\n* **Card styles**: Clean borderless containers with white background card effects.\n\n### Data Model\n\nWe will define a `FOUNDERS_DETAILS` registry in `page.tsx` or a helper file mapping:\n1. `Sam Altman`:\n   * Headline: \"CEO @ OpenAI
<truncated 935 bytes>